require "spec_helper"

describe SpreeCarousel do
  it "has a version number" do
    expect(SpreeCarousel::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
